
using UnityEngine;

public class my2dSpinner : MonoBehaviour
{
    
    void Update()
    {
        /*   // 2 D fordulás:
       float elfordulas = 90;
       Quaternion qElf = Quaternion.Euler(0,0,elfordulas); // eulerként bevittük Quaternionba a Z értékbe
       transform.rotation = qElf;       // egyelõre nem ad ki semmit


       Vector3 h = new Vector3(0,0,90);
       transform.Rotate(h,(elfordulas * Time.deltaTime) * spin );  // nem mûködik
    */
    }
}
